name: helpet_app
description: HelPet app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  google_maps_flutter: ^2.6.0
  geolocator: ^11.0.1
  flutter_map: ^4.0.0          # ✅ VERSIÓN CORRECTA
  latlong2: ^0.8.1             # ✅ VERSIÓN COMPATIBLE
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png